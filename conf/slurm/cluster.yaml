# @package _global_

wandb:
  mode: offline
slurm:
  cluster: Null # becomes None in python
  num_parallel: 50
  update_parameters: # for executor.update_parameters()
    timeout_min: 6000
    slurm_partition: ai, tnt
    gpus_per_node: 1